# AI-Use Appendix – Ritika Neema

## 1. Which parts did you use an assistant for, and which did you write yourself?

I used Claude to generate the initial notebook scaffold covering all three parts (embedding transfer learning, RAG pipeline, optimization techniques). Claude produced the cell structure, markdown explanations, and the bulk of the code. I then ran every cell myself in Google Colab, debugged import/API errors interactively with Claude's help, and updated the RAG failure analysis write-up to match my actual run output.

Specifically, Claude generated:
- Part 1: Word2Vec loading, fine-tuning loop, neighbor extraction, cosine-shift table, and t-SNE visualization.
- Part 2: Wikipedia document loading, LangChain chunking/embedding/FAISS pipeline, the explicit prompt-template + retriever + LLM wiring, retrieval evaluation, and the failure analysis framework.
- Part 3: All five optimization experiments (tensor creation, weight init, activation checkpointing, gradient accumulation, mixed precision) with the shared model/data setup and measurement helpers.

I wrote/modified myself:
- Fixed every compatibility error that came up during execution (see Q2–Q4 below).
- Rewrote the RAG failure report (Section 2.9) to reflect my actual observed failures instead of the pre-written template.
- Chose the 5 RAG questions and the expected-keyword mapping for retrieval evaluation.

## 2. Give one specific thing it produced that was wrong — a tensor shape error, a deprecated API, a loss function that trained but was wrong, a plausible-looking metric computed incorrectly. Paste the wrong output.

Claude's generated code used the `text2text-generation` pipeline task for flan-t5:

```python
generator = hf_pipeline(
    "text2text-generation",
    model="google/flan-t5-base",
    max_new_tokens=200,
)
```

This produced:

```
KeyError: "Unknown task text2text-generation, available tasks are ['any-to-any',
'audio-classification', ... 'text-generation', ...]"
```

The first attempted fix was to switch to `"text-generation"`, which loaded without error but caused a **silent functional failure**: flan-t5 is a seq2seq (encoder-decoder) model, not a causal model. The `text-generation` pipeline treated the prompt as a prefix to continue, so the LLM echoed the entire prompt back instead of generating an answer:

```
Q: Who directed Inception and what is its main plot idea?
A: Answer the question using only the context below. If the answer isn't
in the context, say you don't know.

Context:
Inception is a 2010 science fiction heist film written and directed by
Christopher Nolan...
[entire prompt echoed back]
```

## 3. How did you find out? What did the failure look like?

The `text2text-generation` KeyError was immediately visible as a traceback. The more subtle bug — the `text-generation` pipeline echoing the prompt — was caught when I inspected the Q&A output in Section 2.5. Every "answer" was hundreds of tokens long and started with "Answer the question using only the context below…", which is the prompt template itself, not a generated answer.

## 4. What did you change, and why does your version work?

I bypassed the `transformers.pipeline()` abstraction entirely and loaded flan-t5 directly as a seq2seq model:

```python
from transformers import AutoTokenizer, AutoModelForSeq2SeqLM

t5_tokenizer = AutoTokenizer.from_pretrained("google/flan-t5-base")
t5_model = AutoModelForSeq2SeqLM.from_pretrained("google/flan-t5-base").to("cuda")

def generate_answer(prompt_text, max_new_tokens=200):
    inputs = t5_tokenizer(prompt_text, return_tensors="pt",
                          truncation=True, max_length=512).to(t5_model.device)
    with torch.no_grad():
        outputs = t5_model.generate(**inputs, max_new_tokens=max_new_tokens)
    return t5_tokenizer.decode(outputs[0], skip_special_tokens=True)
```

This works because `AutoModelForSeq2SeqLM` loads the model with its correct architecture (encoder-decoder), and `.generate()` produces a fresh output sequence conditioned on the input — rather than continuing the input as a causal model would. After this fix, answers were concise and correct (e.g. "Christopher Nolan", "Marlon Brando", "RMS Titanic").
